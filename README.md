
# Cardápio Online (v3)
- `/` lista de restaurantes
- `/restaurant/:id` página do restaurante **apenas com categorias**
- `/restaurant/:id/category/:cid` página da **categoria** com produtos

## Rodar
```
npm install
cp .env.example .env.local
# preencha as VITE_FIREBASE_* do Firebase
npm run dev
```
